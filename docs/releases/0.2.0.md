# Vane Search 0.2.0 release checklist

This is a release candidate, not a published release. The real desktop
walkthrough passed on September 23, 2026, and is documented in the
[acceptance record](0.2.0-desktop-acceptance.md). RFC 0003 in the engine
repository requires an updated 0.2.0 demo and a README showing a vault search.
Its remaining official-release evidence must come from this repository's
release URL, not engine staging assets.

## Dependency decision

The demo version is 0.2.0; the bundled engine remains the published,
exact-pinned `@vanedb/wasm` 0.1.1. RFC 0003 does not specify a matching engine
version. This candidate demonstrates the existing semantic-search application
and does not claim the engine's new 0.2.0 APIs or persistence support.

Do not replace the dependency with an unpublished registry version or commit a
local tarball path. If engine 0.2.0 integration is selected for this release:

1. Test the engine candidate tarball in a disposable copy of this repository
   using `npm install --no-save --package-lock=false /path/to/candidate.tgz`,
   then run typecheck, all tests, build, and the size check. Record engine
   commit and tarball checksum; a candidate result does not prove publication.
2. Wait for the approved engine release to publish `@vanedb/wasm@0.2.0`.
3. Pin the published package with `npm install --save-dev --save-exact
   @vanedb/wasm@0.2.0`; commit both package files, rerun all validation, and
   repeat the manual walkthrough with the final release build.

## Manual acceptance — observed; final release identity pending

The [desktop acceptance record](0.2.0-desktop-acceptance.md) records the exact
candidate source, installed build checksum, environment, six synthetic
fixtures, queries, and observed results. In real Obsidian with local Ollama:

- [x] Connection check succeeded and indexing reached six ready chunks.
- [x] The README query ranked Weekend garden first and opened the correct note.
- [x] Full app quit and relaunch preserved the results without manual indexing.
- [x] A saved edit became searchable without manual reindexing.
- [x] Deleting a synthetic note removed it from search; fixtures were restored
      and the original query was rechecked.
- [x] Attach a non-sensitive [screenshot of the recorded query](assets/0.2.0-search-results-20260924.png)
      to the release evidence. The September 24 follow-up reconfirmed the six
      results and accepted bundle checksum; the image contains only synthetic notes.
- [ ] Final reviewed release build has the same checksum as the accepted build,
      or a changed build has passed a fresh desktop walkthrough.

Automated tests use real WASM but fake embedding providers and an Obsidian
stub. The recorded desktop run supplies separate evidence of the actual UI
and model for this fixture; it does not establish relevance for every vault
or behavior on mobile.

## Publication — pending

- All version files and dependency lock agree (`npm run check:release`).
- Clean `npm ci`, typecheck, full tests, build, and size check pass.
- Independent review and required CI checks pass on the final commit.
- Manual acceptance above is reviewed and the final release build identity is
  revalidated against the accepted checksum.
- Release PR is merged; annotated `0.2.0` tag points at the reviewed commit.
- Official release includes `main.js`, `manifest.json`, and `LICENSE`, and its
  README contains the walkthrough.
- Record the official release URL and walkthrough evidence on engine issue
  #242 before claiming the RFC 0003 demo release is complete.
